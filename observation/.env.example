# Copy to .env (gitignored). Entirely separate database/role from
# control-api's — never shared (CLAUDE.md section 5).
DATABASE_URL=postgresql://executive_observation_app:CHANGE_ME@localhost:5432/executive_observation_dev
